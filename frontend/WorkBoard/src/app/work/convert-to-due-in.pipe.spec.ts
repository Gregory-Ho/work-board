import { ConvertToDueInPipe } from './convert-to-due-in.pipe';

describe('ConvertToDueInPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToDueInPipe();
    expect(pipe).toBeTruthy();
  });
});
